# 工作日志 — 2026-05-25

发布 v1.0.8。

## 客户端
- 首次启动语言选择卡片（中 / 英），写入 `state.locale`；覆盖 MSI / 便携 / 解压三种分发渠道
- 便携模式：`portable.flag` 触发，所有数据重定向到 exe 同级 `data/`；标题栏追加标识
- 7z 解压进度条：解析 `-bsp1` 输出驱动 `ProgressBar`，100% 自动收起
- 实时日志彩色高亮：error 红 / warn 黄 / success 绿 / info 蓝 / default
- CSS 不再误扫 Community2024
- A346 OpenList 命名识别：`AS346_*`、`AerosoftA346_*` 都能命中
- AIRAC 取空时自动重试 3 次（3/8/20s）

## 增量更新（全套）
- 文件级 diff + Ed25519 签名 manifest + release.zip 抽取
- updater 模式复用主 exe（`--updater` 参数）
- 30s 心跳超时自动回滚 `.update_backup/`
- 纯 Python Ed25519 验签，不引入运行时 cryptography 依赖
- 仅便携模式启用，MSI 走原 MajorUpgrade

## 工具 / 文档
- `tools/gen_keys.py`、`tools/build_manifest.py`
- `installer/INCREMENTAL_UPDATE.md`：发布流程文档
- `installer/build_signed.bat` 新增第 5/6 步（便携 zip + manifest）
- 版本号 1.0.7 → 1.0.8（state.py / wxs / build_signed.bat）

## 一次性事项
- 生成 Ed25519 主密钥并将公钥写入 `update_keys.PUBLIC_KEY_HEX`
- 私钥位于 `tools/keys/manifest_priv.pem`（已 .gitignore，须本地备份）
